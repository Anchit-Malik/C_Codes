// Write a C program to check whether a given integer is even or odd.
#include<stdio.h>
int main(){
    int a;
    printf("Give any integers values :");
    scanf("%d",&a);
    if (a%2==0)
    {
        printf("The given no. is even");

    }
    else
    {
        printf("The given no. is odd");

    }
    
    
}